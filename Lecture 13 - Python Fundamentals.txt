Python Fundamentals (Variable, data types, conditionals, loops)
#Assign values to variables
name = "John Doe"
age = 30
height = 5.9

#Data Types
num = 10
print(abs(num)) #absolute value
10
print(bin(num)) #binary representation
0b1010
print(hex(num)) #hexadecimal representation
0xa
print(pow(num,2)) #Power function(num^2)
100
print(divmod(num,3)) #Quotient and remainder
(3,1)
price = 99.99 #Float
print(round(price)) #Inbuilt functions and operations for Float
100
print(abs(price)) #absolute value
99.99
print(int(price)) #convert to integer
99
print(float("23.45")) #convert string to float
123.45
print(price.is_integer()) #check if the float is an integer
False
greeting(greeting.lower()) #convert to lower case
hello, world!
greeting(greeting.upper()) #convert to upper case
HELLO, WORLD!
print(greeting.replace("World","Python")) #Replace substring
Hello, Python!
print(greeting.split()) #split string  into a list
['Hello,','World']
print(greeting.find("World")) #Find substring
7
print(len(greeting)) #Length of the string
13
fruits = ["apple","banana","cherry"] #list
fruits.append("orange") #append an element
fruit.extend(["grape","melon"]) #Extend list with another list
fruit.remove("banana") #Remove an element
fruit.pop() #remove last element
fruit.sort() #sort the list
print(len(fruits)) #Length of the list
print(fruits.index("cherry")) #Find index of the element
coordinates = (10.0, 20.0) #Tuple
print(coordinate.count(10.0)) #count occerance of a value
print(coordinate.index(20.0)) #Find index of element
print(len(coordinate)) #Length of the Tuple


#Conditionals (if-else)

#Basic if-elif-else statement
age = 20

if age < 18:
   print("Minor")
elif age >= 18 and age < 65:
   print("Adult")
else:
   print("Senior")
#Output = Adult

#Checking Even opr Odd Number

number = 42

if number % 2 == 0:
    print("Even number")
else:
    print("Odd number")
#Output = Even number

#Grade Evaluation

score = 85

if score >= 90:
    print("Grade: A")
elif score >= 80:
    print("Grade: B")
elif score >= 70:
    print("Grade: C")
elif score >= 60:
    print("Grade: D")
else:
    print("Grade: F")
#Output = Grade: B

#Temperature check

temperature = 30

if temperature > 30:
    print("Its a hot day")
elif temperature >= 20:
    print("Its a nice day")
else:
    print("Its a cold day")
#Output = Its a nice day

#Nested conditionals for BMI Calculation

weight = 70 # in kg
height = 1.75 # in meters

bmi = weight / (height ** 2)

if bmi < 18.5:
    print("Underweight")
else:
    if bmi < 24.9:
        print("Normal weight")
    else:
        if bmi < 29.9:
            print("Overweight")
        else:
            print("Obesity")
#Output = Normal weight

#Multiple Conditions (AND/OR)

temperature = 25
weather = "Sunny"

if temperature > 20 and weather == "Sunny":
      print("Its a great day for a walk")
elif temperature <= 20 or weather != "Sunny":
      print("Maybe stay indoors today")
else:
      print("Weather conditions are unusual")
#Output = Its a great day for a walk

#Loops (for and while loop)

#For Loop
for i in range(5):
    print(i)
#Output = 0
          1
          2
          3
          4
          5

#Iterating Over a List 

fruits = ["apple","banana","cherry","date"]

for fruit in fruits:
      print(fruit)
#Output = apple
          banana
          cherry
          date

#Iterating over a string

greeeting = "Hello"

for char in greeting:
    print(char)
#Output = H
          e
          l
          l
          o

# Iterating Over a Dictionary 

persom = {"name": "John","age":30,"city": "New York"}

for key, value in person.items():
     print(f"{key}: {value}")

#Output = name: John
          age: 30
          city: New York

# Using the 'rangeI()' Function with a step

for i in rangr(0,10,2):
     print(i)
#Output = 0
          2
          4
          6
          8

#Nested For Loops

for i in range(3):
   for j in range(2):
      print(f"i = {i}, j = {j}")
#Output = i = 0, j = 0
          i = 0, j = 1
          i = 1, j = 0
          i = 1, j = 1
          i = 2, j = 0
          i = 2, j = 1

#Using the 'enumerate()' Function

fruit = ["apple","banana","cherry"]

for index, fruit in enumerate(fruits):
    print(f"{index}: {fruit}")
#Output = 0: apple
          1: banana
          2: cherry
#While Loop

count = 0
while count < 5:
    print(count)
    count += 1

#Output = 0
          1
          2
          3
          4

#While Loop with Break

count = 0
while True:
     print(count)
     count += 1
     if count >= 5:
         break
#Output = 0
          1
          2
          3
          4

#While Loop with continue

count = 0
while count < 10:
     count += 1
     if count % 2 == 0:
         continue
     print(count)
#Output = 1
          3
          5
          7
          9



